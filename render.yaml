services:
  - type: web
    name: kairos-advisory-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard.dashboard_dash:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
